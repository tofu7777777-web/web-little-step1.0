services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: littlesteps
      POSTGRES_PASSWORD: littlesteps
      POSTGRES_DB: littlesteps
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: {}
